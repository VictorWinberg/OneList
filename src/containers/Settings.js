import React from 'react';
// import PropTypes from 'prop-types';

const Settings = () => (
  <div>
    <h1>Inställningar</h1>
    <p>Här kan du konfigurera ditt konto</p>
  </div>
);

export default Settings;
